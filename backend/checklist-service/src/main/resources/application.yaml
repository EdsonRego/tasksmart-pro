server:
  port: 8083

spring:
  data:
    mongodb:
      host: ${MONGO_HOST:localhost}
      port: ${MONGO_PORT:27017}
      database: ${MONGO_DATABASE:checklist_db}
      username: ${MONGO_USERNAME:root}
      password: ${MONGO_PASSWORD:root123}
      authentication-database: admin

logging:
  level:
    root: INFO
