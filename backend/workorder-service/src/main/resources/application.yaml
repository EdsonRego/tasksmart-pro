server:
  port: 8084

spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/workorders_db
    username: workorder
    password: workorder123

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    root: INFO
