server:
  port: 8086

spring:
  datasource:
    url: jdbc:postgresql://localhost:5437/assets_db
    username: asset
    password: asset123

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    root: INFO
