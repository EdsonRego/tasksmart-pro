logging:
  level:
    root: INFO

server:
  port: ${SERVER_PORT:0}

admin:
  auth:
    endpoint: ${AUTH_SERVICE_URL:http://localhost:8081}
  users:
    endpoint: ${USER_SERVICE_URL:http://localhost:8082}
  analytics:
    endpoint: ${ANALYTICS_SERVICE_URL:http://localhost:8085}
  notifications:
    endpoint: ${NOTIFICATION_SERVICE_URL:http://localhost:8086}
