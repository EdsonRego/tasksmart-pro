logging:
  level:
    root: INFO

server:
  port: ${SERVER_PORT:0}

reporting:
  analytics:
    endpoint: ${ANALYTICS_SERVICE_URL:http://localhost:8085}
  workorders:
    endpoint: ${WORK_ORDER_SERVICE_URL:http://localhost:8083}
  users:
    endpoint: ${USER_SERVICE_URL:http://localhost:8082}
